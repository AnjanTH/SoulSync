services:
  - type: web
    name: mental-ai-chatbot-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn python.main:app
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0 